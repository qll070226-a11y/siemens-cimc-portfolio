# 升级固件补充说明

可以直接选择 Keil 输出的 `APP\MDK\output\App.bin`。如果文件没有 `5A A5 C3 3C` 魔术字，上位机会弹窗询问是否仅在发送数据前自动补入；磁盘上的原始 bin 不会被修改。

自动补入后总长度仍必须不超过当前 Bootloader 的 48KB 接收缓冲。执行升级前务必确认：目标 APP 地址为 `0x08011000`、Bootloader 与当前模板匹配，并准备调试器恢复方式。
